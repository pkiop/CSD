syms x;
temp = (x+5) * (x^2 + 1.2*x + 9);
expand(temp);

%x^3 + (31*x^2)/5 + 15*x + 45

K = 5 * 0.5 * 9;
G = tf([K],[1 31/5 15 45]);
T = feedback(G, 1)

bode(T)

[M, P, w] = bode(T);
[Mp i] = max(M);
Mp
MpdB = 20 * log10(Mp)
wp = w(i)
for i=1:1:length(M);
    if M(i) <= 0.707;
        fprintf('Bandwidth = %g', w(i))
        break
    end
end    